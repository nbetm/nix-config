# vim: set ft=gitignore :

# OS
.DS_Store
._*
Thumbs.db

# vim backup files
*~

# log files
log/
logs/
*.log
*.log.*

# editors
.idea/
.vscode/
.zed/

# notes
# Deliberately no trailing slash: a pattern ending in / matches directories
# only, and .notes is a symlink into the notes vault in most repos. Git does
# not follow symlinks, so `.notes/` would leave every one of them untracked.
.notes

# direnv
.envrc
.direnv/

# terraform
*tfplan*

# claude
.claude/settings.local.json
.worktrees/
